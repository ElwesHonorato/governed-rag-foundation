[tool.poetry]
name = "ai-ui"
version = "0.1.0"
description = "AI UI service"
authors = ["Governed RAG Foundation"]
package-mode = false

[tool.poetry.dependencies]
python = "3.11.14"
flask = "^3.1.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
