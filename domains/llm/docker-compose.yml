services:
  ollama:
    build:
      context: ./domains/llm
      dockerfile: Dockerfile
      args:
        LLM_MODEL: ${LLM_MODEL:?LLM_MODEL is required}
    ports:
      - "${LLM_PORT:?LLM_PORT is required}:11434"

networks:
  default:
    external: true
    name: rag-local
