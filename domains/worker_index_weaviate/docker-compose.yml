services:
  pipeline-worker-index-weaviate:
    build:
      context: .
      dockerfile: ./domains/worker_index_weaviate/Dockerfile
    environment:
      WEAVIATE_URL: ${WEAVIATE_URL:?WEAVIATE_URL is required}
      BROKER_URL: ${BROKER_URL:?BROKER_URL is required}
      S3_ENDPOINT: ${S3_ENDPOINT:?S3_ENDPOINT is required}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:?S3_ACCESS_KEY is required}
      S3_SECRET_KEY: ${S3_SECRET_KEY:?S3_SECRET_KEY is required}
      DATAHUB_GMS_SERVER: ${DATAHUB_GMS_SERVER:-${DATAHUB_GMS_URL:-http://datahub-gms:8080}}
      DATAHUB_ENV: ${DATAHUB_ENV:?DATAHUB_ENV is required}
      DATAHUB_TOKEN: ${DATAHUB_TOKEN:-}
      SPARK_ENABLED: ${SPARK_ENABLED:-false}
      SPARK_MASTER_URL: ${SPARK_MASTER_URL:-local[*]}
      SPARK_APP_NAME: ${SPARK_APP_NAME:-worker-process}

networks:
  default:
    external: true
    name: ${STACK_NETWORK:?STACK_NETWORK is required}
