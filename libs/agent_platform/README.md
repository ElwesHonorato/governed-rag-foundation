# Agent Platform

MVP capability-oriented agent platform library package.

## Runtime Requirements

The package sends `llm_synthesize` requests to a real Ollama-compatible LLM backend.

Required environment variables:
- `LLM_URL`
- `LLM_MODEL`
- `LLM_TIMEOUT_SECONDS`
