# Tooling

Repository-level support utilities are organized by purpose:

- `ops/`: stack and runtime lifecycle operations.
- `python_env/`: local developer automation helpers.
- `ci/`: CI-focused helper tooling.
